name: Test Image

on:
  push:
    branches: [ long-lived ]

jobs:
  test:
    name: ${{matrix.runs-on}}
    strategy:
      fail-fast: false
      matrix:
        runs-on: [ubicloud-standard-2-ubuntu-2204]
    runs-on: ${{matrix.runs-on}}

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - run: sleep 10000000